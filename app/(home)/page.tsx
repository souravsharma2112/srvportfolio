import { AboutSection, HeroSection, ServicesSection, WorkSection } from './_components'

const page = () => {
  return (
    <main>
      <HeroSection />
      <AboutSection />
      <ServicesSection />
      <WorkSection />
    </main>
  )
}

export default page
